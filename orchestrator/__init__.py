"""Relay — routes a natural-language coding task to the cheapest capable agent CLI."""

# Single source of truth for the version: pyproject reads it from here
# (dynamic metadata) and the REPL banner prints it, so the two cannot drift.
__version__ = "0.2.0"
